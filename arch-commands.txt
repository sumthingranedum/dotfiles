archinstall
mirrors
grub
user
minimal
neovim git
pipewire
network manager
time zone

paru
git clone https://aur.archlinux.org/paru.git
cd paru
makepkg -si
rustup
rustup default stable
makepkg -si

paru -S neofetch hyprland wl-clipboard kitty firefox ttc-iosevka hyprpaper xdg-user-dirs virt-manager qemu-full dnsmasq nwg-look-bin polkit-kde-agent qt5-wayland qt6-wayland tldr

sudo systemctl enable libvirtd.socket
sudo systemctl start libvirtd.socket

sudo virsh net-autostart default

sudo usermod -aG libvirt zack

firefox
about:config
ui.key.menuAccessKeyFocuses = false
general.autoScroll = true
remove bloat from toolbar

surface kernel
curl -s https://raw.githubusercontent.com/linux-surface/linux-surface/master/pkg/keys/surface.asc \
    | sudo pacman-key --add -

sudo pacman-key --finger 56C464BAAC421453

sudo pacman-key --lsign-key 56C464BAAC421453

add:
[linux-surface]
Server = https://pkg.surfacelinux.com/arch/
to the end of /etc/pacman.conf

paru

paru -S linux-surface linux-surface-headers iptsd libwacom-surface

sudo grub-mkconfig -o /boot/grub/grub.cfg

reboot

uname -a
